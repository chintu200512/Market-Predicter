apiVersion: v1
kind: Service

metadata:
  name: market-predicter-service

spec:
  type: NodePort

  selector:
    app: market-predicter

  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 4040
